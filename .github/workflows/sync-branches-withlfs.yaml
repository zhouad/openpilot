name: Sync External Branches (debug)

on: [workflow_dispatch]

jobs:
  debug-ssh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Write private key to file (from secret)
        run: |
          mkdir -p ~/.ssh
          cat > ~/.ssh/id_deploy <<'EOF'
          ${{ secrets.SSH_PRIVATE_KEY }}
          EOF
          chmod 600 ~/.ssh/id_deploy
          echo ">>> private key first line:"
          head -n 1 ~/.ssh/id_deploy

      - name: Start ssh-agent and add key
        run: |
          eval "$(ssh-agent -s)"
          ssh-add ~/.ssh/id_deploy || true
          ssh-add -l || true

      - name: Ensure known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts
          echo "known_hosts created"

      - name: Test SSH auth
        run: |
          ssh -i ~/.ssh/id_deploy -o StrictHostKeyChecking=no -T git@github.com || true

      - name: Show git remotes
        run: git remote -v || true

      - name: Try a dry-run push (no credentials leaked)
        run: |
          git remote set-url origin git@github.com:zhouad/openpilot.git || true
          git fetch --all || true
          git push origin motn-0912-vibe --dry-run || true
